import React from 'react';
type style = {
    image: string;
    children: JSX.Element;
    customStyles?: string;
};
export declare const Background1: ({ children, image, customStyles }: style) => React.JSX.Element;
export {};
